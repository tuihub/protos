syntax = "proto3";
package sentinel;
option go_package = "tuihub.com/librarian/v2";

service Greeter {
    rpc Report (ReportReq) returns (ReportResp);
}

message ReportReq {
    string token = 1;
    repeated string list = 2;
}

message ReportResp {
    bool status = 1;
}